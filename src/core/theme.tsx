import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#44C8E8",
    secondary: "#414757",
    error: "#f13a59",
  },
};

export default theme;
